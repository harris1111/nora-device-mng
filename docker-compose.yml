services:
  app:
    build: .
    ports:
      - "${PORT:-13000}:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - BASE_URL=${BASE_URL:-http://localhost:13000}
    volumes:
      - device-data:/app/data
    restart: unless-stopped

volumes:
  device-data:
